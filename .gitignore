# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/.playwright-mcp/
*.png

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files — .env.example is safe to commit (no real values), all others are not
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

*storybook.log
storybook-static
.vercel

# hackathon research track — separate from the main product, not committed
docs/AI_HACKATHON_HEALTHCARE.md

# design-sync (claude.ai/design import)
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/

# premium reference PDFs (purchased content — digests live in docs/, originals stay local)
docs/research/extracts/

# eval run history — local/ephemeral output, not repo state (PRACTICES §6)
eval/results/

# real anonymised eval documents — never committed to this public repo
# (D-016). Convention: real case directories are named "real-*", synthetic
# placeholders stay "synthetic-*" and stay tracked/committed as-is.
eval/cases/real-*/

# internal-only docs (competitive research, strategy notes) — never committed
# to this public repo; a decision record still exists in DECISIONS.md, just
# without the underlying competitive detail
docs/private/
