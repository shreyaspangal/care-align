{
  "comment": "Bundle-only tsconfig for /design-sync. Keeps the @/ alias and swaps Next's client runtime for browser stubs so esbuild can produce a self-contained IIFE. baseUrl defaults to this file's dir (.design-sync), so ../* resolves to repo root.",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["../*"],
      "next/navigation": ["./stubs/next-navigation.tsx"],
      "next/link": ["./stubs/next-link.tsx"]
    }
  }
}
